<div class="bSpinner" style="top: -40px; left: -40px;"></div><!-- Remember to include excanvas for IE8 chart support -->
<!--[if IE 8]>
<script src="/account/js/helpers/excanvas.min.js"></script><![endif]-->
<!-- jQuery, Bootstrap.js, jQuery plugins and Custom JS code -->
<script src="/account/js/vendor/jquery-1.11.3.min.js"></script>
<script src="/account/js/vendor/jquery.cookie.js"></script>
<script src="/account/js/vendor/bootstrap.min.js"></script>
<script src="/account/js/plugins.js"></script>
<script src="/account/js/app.js"></script>
<script src="/account/js/helpers/rating.jquery.js"></script>

#if($!authmenu)
<!-- Load and execute javascript code used only in this page -->
<script src="/account/js/pages/login.js"></script>
<script src="https://www.google.com/recaptcha/api.js?hl=en&onload=onloadCallback&amp;render=explicit" async defer></script>
<script>$(function(){ Login.init(); });</script>
<script type="text/javascript">
     var onloadCallback = function() {  mysitekey = '$!recaptchaKeySite'; recaptcha1_key = grecaptcha.render('recaptcha1', {  'sitekey': mysitekey }); recaptcha2_key = grecaptcha.render('recaptcha2', {  'sitekey': mysitekey }); recaptcha3_key = grecaptcha.render('recaptcha3', {  'sitekey': mysitekey /*'theme' : 'dark', //default - light 'type' : 'audio', //default - image 'size' : 'compact', //default - normal 'tabindex' : 1, //default - 0 'callback' : , //function on success 'expired-callback' : //function when response expires*/ }); };
</script>
#end
#if($!mainmenu)
<script>
NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   var click_update = false; var timerId = setTimeout(function() { click_update = true;$('.update-balance').css( "color", "#27ae60" ); clearTimeout(timerId); }, 5000); $('body').on('click', '.balance-animation', function (e) {  balance_update(); }); function balance_update(){  if(click_update){  $('.balance-animation').addClass('active'); $.ajax({  url: "/data/controllers", data: 'module=head_wedget_main&action=balance_update', type: "POST", cache: false, dataType: 'json', beforeSend: function () {  click_update = false; $('.update-balance').css( "color", "#e6ea2d" ); NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  click_update = true; $('.update-balance').css( "color", "#27ae60" ); $('.balance-animation').removeClass('active'); NProgress.done(); } }).done(function (msg) {  if(msg.status == 'success'){  $('.balance-animation strong').text(msg.text); }else if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.eval){  jQuery.globalEval( msg.eval ); } }).always(function () {  $('.balance-animation').removeClass('active'); $('.update-balance').css( "color", "red" ); click_update = false; timerId = setTimeout(function() { click_update = true; $('.update-balance').css( "color", "#27ae60" ); clearTimeout(timerId); }, 5000); NProgress.done(); }); } }  $('img').error(function(){  this.src='.png'; });  $('.information-window').on('change', '.pin-shield', function (e) {  var window = $(this).data('window'); if($(this).is(':checked')) {  $.ajax({  url: "/data/controllers", data: 'module=global_informaiton_block&action=pin_enable', type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); $('input.pin-shield').trigger('click'); } }).done(function (msg) {  if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); if(msg.status != 'success'){  $('#pin-shield').removeClass('pin-shield'); $('#pin-shield').trigger('click'); $('#pin-shield').addClass('pin-shield'); } } if(msg.eval){  jQuery.globalEval( msg.eval ); } }).always(function () {  NProgress.done(); }); }else{  if( window != undefined ) {  $('a.button-pin-shield').trigger('click'); ajax_information_block( window ); } } }); $('.information-window').on('click', '.information-window-open', function (e) {  var window = $(this).data('window'); if( window != undefined ) {  ajax_information_block( window ); } }); function ajax_information_block ( type ){  $.ajax({  url: "/data/controllers", data: 'module=global_informaiton_block&open=' + type, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  $('.title-information-panel').html('Loding...'); $('#body-information-panel').html('<p style="min-height: 200px;padding-top: 70px;" class="text-center"><i style="width: 100%;" class="gi gi-roundabout fa-spin fa-5x text-success"></i><p>'); } }).done(function (msg) {  $('.title-information-panel').html(''); $('.title-information-panel').append(msg.title); $('#body-information-panel').html(''); $('#body-information-panel').append(msg.html); }); }  $('.account-list-char').on('click', '.btn-account-char', function (e) {  var change = $(this).data('change'); var recovery = $(this).data('recovery'); if( change != undefined ) {  $('.name-account-modal').html(change); ajax_account_list_char( change , 'change'); } else if( recovery != undefined ) {  $('.name-account-modal').html(recovery); ajax_account_list_char( recovery, 'recovery'); } }); function ajax_account_list_char (data , type){  $.ajax({  url: "/data/controllers", data: 'module=account_list_all&open=' + type + '&account_name=' + data, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  $('#body-'+type+'-password').html('<p style="min-height: 200px;padding-top: 70px;" class="text-center"><i style="width: 100%;" class="gi gi-roundabout fa-spin fa-5x text-success"></i><p>'); } }).done(function (msg) {  $('#body-'+type+'-password').html(''); $('#body-'+type+'-password').append(msg.html); }); } $('body').on('click', '.btn-refresh-char', function (e) {  $.ajax({  url: "/data/controllers", data: 'module=account_list_all&account_list_refresh=1', type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); } }).done(function (msg) {  if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); }else{  if (response_loc === undefined) $('.show').html(msg.text); else $('.show_' + response_loc).html(msg.text); } if(msg.location){  setTimeout("document.location.href='/"+msg.location+"'", msg.time); } if(msg.eval){  jQuery.globalEval( msg.eval ); } NProgress.done(); }); });   $('.register-prefix').click(function(){ $.get("/data/prefix/get",function (data){ $('#register-prefix').html(data); $('#register-prefix-input').val(data);});}); $('#form-register-game-account').validate({  errorClass: 'help-block animation-slideDown', errorElement: 'div', errorPlacement: function(error, e) {  e.parents('.form-group').append(error); }, highlight: function(e) {  $(e).closest('.form-group').removeClass('has-success has-error').addClass('has-error'); $(e).closest('.help-block').remove(); }, success: function(e) {  e.closest('.form-group').removeClass('has-success has-error'); e.closest('.help-block').remove(); }, rules: {  'register-account': {  required: true, minlength: 6 }, 'register-password': {  required: true, minlength: 8 }, 'register-password-verify': {  required: true, equalTo: '#register-password' } }, messages: {  'register-account': {  required: 'Please enter your Login', minlength: 'Your Login must be at least 6 characters long' }, 'register-password': {  required: 'Please enter a password', minlength: 'Your password must be at least 8 characters long' }, 'register-password-verify': {  required: 'Please enter a password', minlength: 'Your password must be at least 8 characters long', equalTo: 'Please enter the same password as above' }, } });  $('#form-add-game-account').validate({  errorClass: 'help-block animation-slideDown', errorElement: 'div', errorPlacement: function(error, e) {  e.parents('.form-group').append(error); }, highlight: function(e) {  $(e).closest('.form-group').removeClass('has-success has-error').addClass('has-error'); $(e).closest('.help-block').remove(); }, success: function(e) {  e.closest('.form-group').removeClass('has-success has-error'); e.closest('.help-block').remove(); }, rules: {  'register-account': {  required: true, minlength: 6 }, 'register-password': {  required: true, minlength: 8 }, }, messages: {  'register-account': {  required: 'Please enter your Login', minlength: 'Your Login must be at least 6 characters long' }, 'register-password': {  required: 'Please enter a password', minlength: 'Your password must be at least 8 characters long' } } });       }); NProgress.done();
</script>
#end
#if($!settingsmenu)
<script>
NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });            }); NProgress.done();
</script>
#end
#if($!logmenu)
<script> NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   var TablesDatatables = function() {  return {  init: function() {  App.datatables(); $('#example-datatable').dataTable({  columnDefs: [ { orderable: false, targets: [ 1, 4 ] } ], pageLength: 15, lengthMenu: [[15, 20, 30, -1], [15, 20, 30, 'All']] }); $('.dataTables_filter input').attr('placeholder', 'Search'); } }; }(); $(function(){ TablesDatatables.init(); });        $('#open-modal-notice-18496').trigger('click');   $('.poll_project').rating({messages: ['What did you think?','Terribly!','It leaves something to be desired','Didn\'t surprise me','I liked it','It\'s the best!']}).change(function () {
                            App.ajax_send('module=module_notice&rating=' + $(this).rating('val') + '&notice_id=18496');});      }); NProgress.done();
</script>
#end
#if($!supportmenu)
<script> NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '.new-tiket-standart', function () {  $('[href = "#tickets-new"]').parent().remove(); $('#tickets-new').remove(); $('[href = "#tickets-list"]').trigger('click'); ajax_open_block("tickets-new" , "tickets-new"); }); /*donat*/ $('body').on('click', '.new-tiket-quick-app', function () {  $('[href = "#tickets-new"]').parent().remove(); $('#tickets-new').remove(); $('[href = "#tickets-list"]').trigger('click'); ajax_open_block("tickets-new" , "tickets-new-quick-app"); }); /*bot*/ $('body').on('click', '.new-tiket-quick-app-bot', function () {  $('[href = "#tickets-new"]').parent().remove(); $('#tickets-new').remove(); $('[href = "#tickets-list"]').trigger('click'); ajax_open_block("tickets-new" , "tickets-new-quick-app-bot"); }); /*drop item*/ $('body').on('click', '.tickets-new-quick-drop-itemt', function () {  $('[href = "#tickets-new"]').parent().remove(); $('#tickets-new').remove(); $('[href = "#tickets-list"]').trigger('click'); ajax_open_block("tickets-new" , "tickets-new-quick-drop-item"); }); /*open tickets*/ $('body').on('click', '.open-tickets', function () {  var idt = $(this).data("tickets"); $('[href = "#tickets-windows"]').parent().remove(); $('#tickets-windows').remove(); ajax_open_block("tickets-windows" , "open-tickets&idt="+idt); }); /*tickets comment*/ $('body').on('click', '[data-tid]', function () {  var comment_id = $(this).data("tid"); var comment_text = $( ".comment_text_" + comment_id ).html(); $('#tickets-reply').val( $('#tickets-reply').val()+'\n > '+comment_text+'\n'); }); $(".nav-tabs").on("click", "a", function(e){  e.preventDefault(); $(this).tab('show'); }) .on("click", ".remove-tab", function () {  var anchor = $(this).parent('a'); console.log(anchor.attr('href')); $(anchor.attr('href')).remove(); $(this).parent().parent().remove(); $(".nav-tabs li").children('a').first().click(); }); function ajax_open_block ( act , options){  $.ajax({  url: "/data/controllers", data: 'module=block_wedget_support&act=' + options, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); } }).done(function (msg) {  if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); }else if( msg.title != undefined ){  $('.nav-tabs').append('<li><a href="#'+act+'">'+ msg.title +' <i class="fa fa-times remove-tab"></i></a></li>'); $('.tab-content').append( '<div class="tab-pane" id="'+act+'">'+ '<div class="block-content-full">' +msg.html+ '</div></div>' ); $('[href = "#'+act+'"]').trigger('click'); }else{  console.log(msg); } if(msg.location){  setTimeout("document.location.href='/"+msg.location+"'", msg.time); } if(msg.eval){  jQuery.globalEval( msg.eval ); } NProgress.done(); }); } $('body').on('change', '#account_name', function (e) {  var account_id_service = $( this ).val(); if (account_id_service != '0') {  $( '#char_id' ).find( 'option' ).remove().end().prop( 'disabled',true ); var check = true; for ( var i = 0; i < char_support[account_id_service].length; i++ ) {  if(char_support[account_id_service][i].value != 0 && check) {  $('#char_id').append('<option value="0">Choose your character.</option>'); check = false; } $( '#char_id' ).append( '<option value="' + char_support[account_id_service][i].value + '">' + char_support[account_id_service][i].name + '</option>' ); } $( '#char_id' ).prop( 'disabled', false ); } });        $('.poll_project').rating({messages: ['What did you think?','Terribly!','It leaves something to be desired','Didn\'t surprise me','I liked it','It\'s the best!']}).change(function () {
                            App.ajax_send('module=module_notice&rating=' + $(this).rating('val') + '&notice_id=18496');});      }); NProgress.done();
</script>
#end
#if($!donatemenu)
<script>
     NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '.btn-bonus-percent', function (e) {  $(this).removeClass('btn-bonus-percent'); $(this).addClass('submit-btn'); $('.impute-hide').fadeIn('slow'); }); $('body').on('change', '#account-bonus-percent', function (e) {  var account_id = $( this ).val(); if (account_id == '0') {  $("#char-list-bonus-percent").fadeOut('slow'); }else{  $("#char-list-bonus-percent").fadeIn('slow'); $( '#select-char-id-bonus' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list[account_id].length; i++ ) {  $( '#select-char-id-bonus' ).append( '<option value="' + char_list[account_id][i].value + '">' + char_list[account_id][i].name + '</option>' ); } $( '#select-char-id-bonus' ).prop( 'disabled', false ); } }); $('body').on('click', '.window-percent-open', function (e) {  var window = $(this).data('percent'); if( window != undefined ) {  ajax_modal_percent_bonus_block( window ); } }); function ajax_modal_percent_bonus_block ( type ){  $.ajax({  url: "/data/controllers", data: 'module=head_wedget_donat_percent&open=' + type, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); $('.ajax-loud-window').html('<p style="min-height: 200px;padding-top: 70px;" class="text-center"><i style="width: 100%;" class="gi gi-roundabout fa-spin fa-5x text-success"></i><p>'); } }).done(function (msg) {  $('.ajax-loud-window').html(''); $('.ajax-loud-window').append(msg.html); NProgress.done(); }); }
var donat_type = "unitpay"; $('body').on('click', '.donat-pay', function () {  donat_type = $(this).data('pay'); $('.donat-pay').removeClass("enable-donate animation-pulse"); $(this).addClass("enable-donate animation-pulse"); change_balance(); $('#method-pay').val(donat_type); }); onsubmit="return false;"
     $('body').on('click change', '#enter_sum', function () {  change_balance(); }); function change_balance(){  var enter_sum = $('#enter_sum').val(); var bonus = 0; var bar_val = 0; if (enter_sum >= 300 && enter_sum < 1000) {  bonus = Math.round(enter_sum * 3 / 100); bar_val = 1; } else if (enter_sum >= 1000 && enter_sum < 2000) {  bonus = Math.round(enter_sum * 5 / 100); bar_val = 2; } else if (enter_sum >= 2000 && enter_sum < 3500) {  bonus = Math.round(enter_sum * 10 / 100); bar_val = 3; } else if (enter_sum >= 3500 && enter_sum < 7000) {  bonus = Math.round(enter_sum * 15 / 100); bar_val = 4; } else if (enter_sum >= 7000) {  bonus = Math.round(enter_sum * 20 / 100); bar_val = 5; } else {  bonus = 0; bar_val = 0; } $('#bonus').html( bonus); $('#sum').val( enter_sum);     var bars = $('.checkout-bar li'); bars.each(function(){this.className = ""}); for(var i=0; i<bar_val; i++){ bars[i].className = "visited"; } if(bar_val!=0){bars[bar_val-1].className = "active";}    }  $('body').on('change', '#account-bonus-cod', function (e) {  var account_id_cod = $( this ).val(); if (account_id_cod == '0') {  $("#char-list-bonus-cod").fadeOut('slow'); }else{  $("#char-list-bonus-cod").fadeIn('slow'); $( '#select-char-id-bonus-cod' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus[account_id_cod].length; i++ ) {  $( '#select-char-id-bonus-cod' ).append( '<option value="' + char_list_bonus[account_id_cod][i].value + '">' + char_list_bonus[account_id_cod][i].name + '</option>' ); } $( '#select-char-id-bonus-cod' ).prop( 'disabled', false ); } });       }); NProgress.done();
</script>
#end
#if($!shopmenu)
<script>
     NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   var click_update = false; var timerId = setTimeout(function() { click_update = true;$('.update-balance').css( "color", "#27ae60" ); clearTimeout(timerId); }, 5000); $('body').on('click', '.balance-animation', function (e) {  balance_update(); }); function balance_update(){  if(click_update){  $('.balance-animation').addClass('active'); $.ajax({  url: "/data/controllers", data: 'module=head_wedget_main&action=balance_update', type: "POST", cache: false, dataType: 'json', beforeSend: function () {  click_update = false; $('.update-balance').css( "color", "#e6ea2d" ); NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  click_update = true; $('.update-balance').css( "color", "#27ae60" ); $('.balance-animation').removeClass('active'); NProgress.done(); } }).done(function (msg) {  if(msg.status == 'success'){  $('.balance-animation strong').text(msg.text); }else if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.eval){  jQuery.globalEval( msg.eval ); } }).always(function () {  $('.balance-animation').removeClass('active'); $('.update-balance').css( "color", "red" ); click_update = false; timerId = setTimeout(function() { click_update = true; $('.update-balance').css( "color", "#27ae60" ); clearTimeout(timerId); }, 5000); NProgress.done(); }); } } var char_list_shop = new Object(); var item_list_shop = new Object();  #foreach($account in $accounts) char_list_shop["$account.account"] = new Array( #foreach($player in $players)#if($player.accountId == $account.id)#if($result),#end {value:'$player.id',name:'$player.name'}#set($result=true)#end#end ); #end  function pad(number, length) {  var str = '' + number; while (str.length < length) { str = '0' + str;} return str; } function formatTime(time) {  var second = 1000; var minute = second * 60; var hour = minute * 60; var day = hour * 24; var days = Math.floor(time / day); var hours = Math.floor((time %day) / hour ); var minutes = Math.floor((time % hour) / minute); var seconds = Math.floor((time % minute) / second); var str = ''; if(days > 0) str += days + "day "; str += pad(hours, 2) + ":"; str += pad(minutes, 2); str += ":" + pad(seconds, 2); return str; } $('body').on('click', '.window-shop-open', function (e) {  var type = $(this).data('shop'); if( type != undefined ) {  $.ajax({  url: "/data/controllers", data: 'module=block_shop&open=' + type, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); $('.ajax-loud-window').html('<p style="min-height: 200px;padding-top: 70px;" class="text-center"><i style="width: 100%;" class="gi gi-roundabout fa-spin fa-5x text-success"></i><p>'); } }).done(function (msg) {  $('.ajax-loud-window').html(''); $('.ajax-loud-window').append(msg.html); NProgress.done(); }); } }); $('body').on('click', '.next_step', function (e) {  var step = $(this).attr('step'); if(step == 2){  if($("input:checkbox:checked").val() != 'on'){  $('#error_item_msg').text('You must seect the items!'); return false; }else $('#error_item_msg').text(''); }else if(step == 3){  if($("input[name=account]:radio:checked").val() == undefined || $("input[name=char]:radio:checked").val() == undefined || $("input[name=char]:radio:checked").val() == 0){  $('#error_account_msg').text('You must choose the character who will receive the items!'); return false; }else $('#error_account_msg').text(''); } $('.step_' + (parseInt(step) - 1)).hide(); $('.step_' + step).show(); }); $('body').on('click', '.back_step', function (e) {  var step = $(this).attr('step'); $('.step_' + (parseInt(step) + 1)).hide(); $('.step_' + step).show(); }); $('body').on('click', 'input:checkbox', function (e) {  var price = $(this).attr('price'); var item_name = $(this).attr('item_name'); var id_item = $(this).attr('id'); if(item_list_shop[id_item] == undefined) {  item_list_shop[id_item] = item_name; } else {  delete item_list_shop[id_item]; } $("#item_list").text(''); for (var x in item_list_shop) $("#item_list").append(item_list_shop[x] + "<br>"); $(this).attr('price', parseInt(price)*-1); var summ = $('.price_all').text(); $('.price_all').text(parseInt(summ)+parseInt(price)); $('.price_all_finish').text(parseInt(summ)+parseInt(price)); }); $('body').on('click', '.account input:radio', function (e) {  var account = $(this).attr('value'); $( '.bay_account' ).text(account); $( '.chars' ).text(''); for ( var i = 0; i < char_list_shop[account].length; i++ ) {  $( '.chars' ).append( '<div class="select_item-success">'+ '<input type="radio" name="char" value="' + char_list_shop[account][i].value + '" char_name="' + char_list_shop[account][i].name + '" id="radio' + i + '">'+ '<label for="radio' + i + '">' + char_list_shop[account][i].name + '</label>'+ '</div>' ); } }); $('body').on('click', '.chars input:radio', function (e) {  var chars = $(this).attr('char_name'); $( '.bay_char' ).text(chars); }); $('body').on('click', 'select_item input:checkbox', function (e) {  var table = $(this).closest('select_item'); $('select_item input:checkbox', table).each(function() {  console.log($(this).attr('id')); if($(this).prop('checked')){  } }); });   ;(function($) {  $.timer = Timer; /** * First parameter can either be a function or an object of parameters. * * @param { function | {  * action: function, * time: int=, * autostart: boolean= * }} action * @param { int=} time * @param { boolean=} autostart * @returns { Timer} */ function Timer(action, time, autostart) {  if (this.constructor != Timer || this.init) {  return new Timer(action, time, autostart); } this.set(action, time, autostart); return this; } /** * @see Timer * * @param { function | {  * action: function, * time: int=, * autostart: boolean= * }} action * @param { int=} time * @param { boolean=} autostart * @returns { Timer} */ Timer.prototype.set = function(action, time, autostart) {  this.init = true; if (typeof action == "object") {  if (action.time) {  time = action.time; } if (action.autostart) {  autostart = action.autostart; } action = action.action; } if (typeof action == "function") {  this.action = action; } if (!isNaN(time)) {  this.intervalTime = time; } if (autostart && this.isReadyToStart()) {  this.isActive = true; this.setTimer(); } return this; }; Timer.prototype.isReadyToStart = function() {  var notActive = !this.active; var hasAction = typeof this.action == "function"; var hasTime = !isNaN(this.intervalTime); return notActive && hasAction && hasTime; }; /** * @param { int=} time * @returns { Timer} */ Timer.prototype.once = function(time) {  var timer = this; if (isNaN(time)) {  timer.action(); return this; } setTimeout(fnTimeout, time); return this; function fnTimeout() {  timer.action(); } }; /** * @param { boolean=} reset * @returns { Timer} */ Timer.prototype.play = function(reset) {  if (this.isReadyToStart()) {  if (reset) {  this.setTimer(); } else {  this.setTimer(this.remaining); } this.isActive = true; } return this; }; /** * @returns { Timer} */ Timer.prototype.pause = function() {  if (this.isActive) {  this.isActive = false; this.remaining -= new Date() - this.last; this.clearTimer(); } return this; }; /** * @returns { Timer} */ Timer.prototype.stop = function() {  this.isActive = false; this.remaining = this.intervalTime; this.clearTimer(); return this; }; /** * @param { boolean=} reset * @returns { Timer} */ Timer.prototype.toggle = function(reset) {  if (this.isActive) {  this.pause(); } else if (reset) {  this.play(true); } else {  this.play(); } return this; }; /** * @returns { Timer} */ Timer.prototype.reset = function() {  this.isActive = false; this.play(true); return this; }; /** * @returns { Timer} */ Timer.prototype.clearTimer = function() {  clearTimeout(this.timeoutObject); return this; }; /** * @returns { Timer} */ Timer.prototype.setTimer = function(time) {  var timer = this; if (isNaN(time)) {  time = this.intervalTime; } this.remaining = time; this.last = new Date(); this.clearTimer(); this.timeoutObject = setTimeout(fnTimeout, time); return this; function fnTimeout() {  timer.execute(); } }; /** * @returns { Timer} */ Timer.prototype.execute = function() {  if (this.isActive) {  try {  this.action(); } finally {  this.setTimer(); } } return this; }; })(jQuery);        }); NProgress.done();
</script>
#end
#if($!transfermenu)
<script>
NProgress.start(); $(document).ready(function() {    $('body').on('change', '#account-bonus-cod-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bonus-cod-modal").fadeOut('slow'); }else{  $("#char-list-bonus-cod-modal").fadeIn('slow'); $( '#select-char-id-bonus-cod-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bonus_modal[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bonus-cod-modal' ).append( '<option value="' + char_list_bonus_modal[account_id_cod_modal][i].value + '">' + char_list_bonus_modal[account_id_cod_modal][i].name + '</option>' ); } $( '#select-char-id-bonus-cod-modal' ).prop( 'disabled', false ); } });  $('body').on('change', '#account-bay-coin-modal', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-bay-coin-modal").fadeOut('slow'); }else{  $("#char-list-bay-coin-modal").fadeIn('slow'); $( '#select-char-id-bay-coin-modal' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-bay-coin-modal' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-bay-coin-modal option' ).val(); if (char_id_cod_modal == '0') {   $("#button-bay-coin").fadeOut('slow');  }else{   $("#button-bay-coin").fadeIn('slow');  } $( '#select-char-id-bay-coin-modal' ).prop( 'disabled', false ); }  }); $('body').on('change keyup paste click', '#input-pin-code', function (e) {  var new_name_service = $( this ).val(); if (/[0-9][0-9][0-9][0-9]$/.test(new_name_service) === false ) {  $("#button-bay-coin").fadeOut('slow'); }else {  $("#button-bay-coin").fadeIn('slow'); } });  $('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });   $('body').on('click', '[server]', function () {  var server = $(this).attr('server'); $.ajax({  url: "/data/controllers", data: 'module=select_server&server='+ server, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('server', server, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });
  $('body').on('change', '#account-buy-coin', function (e) {  var account_id_cod_modal = $( this ).val(); if (account_id_cod_modal == '0') {  $("#char-list-buy-coin").fadeOut('slow'); }else{  $("#char-list-buy-coin").fadeIn('slow'); $( '#select-char-id-buy-coin' ).find( 'option' ).remove().end().prop( 'disabled',true ); for ( var i = 0; i < char_list_bay_coin[account_id_cod_modal].length; i++ ) {  $( '#select-char-id-buy-coin' ).append( '<option value="' + char_list_bay_coin[account_id_cod_modal][i].value + '">' + char_list_bay_coin[account_id_cod_modal][i].name + '</option>' ); } var char_id_cod_modal = $( '#char-list-buy-coin option' ).val(); if (char_id_cod_modal == '0') {   $("#button-buy-coin").fadeOut('slow');  }else{   $("#button-buy-coin").fadeIn('slow');  } $( '#select-char-id-buy-coin' ).prop( 'disabled', false ); }  });        }); NProgress.done();
</script>
#end
#if($!isAdmin && $!adminmenu)
<script>
NProgress.start(); $(document).ready( function() { $('body').on("click", '.submit-multipart', function(){  var el = $(this);  var action = el.parents('form').attr('action');  var formData = new FormData((el.parents('form'))[0]);   $.ajax({  url: action,  data: formData,  type: "POST",  cache: false, dataType: 'json', contentType: false, processData: false,  beforeSend: function () {   NProgress.start();   el.attr("disabled","disabled");  },  error: function (xhr, ajaxOptions, thrownError) {   NProgress.done();   console.log("Result : " + xhr.responseText);  }  }).done(function (msg) {  if(msg.type == 'msg'){   $.bootstrapGrowl( msg.text , {   type: msg.status,   delay: msg.time,   allow_dismiss: true   });  }  if(msg.location){   setTimeout("document.location.href='/"+msg.location+"'", msg.time);  }  if(msg.eval){   jQuery.globalEval( msg.eval );  }  }).always(function () {  NProgress.done();  setTimeout(function () {   el.removeAttr("disabled");  }, 500);  }); });
/*drop item*/ $('body').on('click', '.tickets-new-quick-drop-itemt', function () {  $('[href = "#tickets-new"]').parent().remove(); $('#tickets-new').remove(); $('[href = "#tickets-list"]').trigger('click'); ajax_open_block("tickets-new" , "tickets-new-quick-drop-item"); });
/*open tickets*/ $('body').on('click', '.open-tickets', function () {  var idt = $(this).data("tickets"); $('[href = "#tickets-windows"]').parent().remove(); $('#tickets-windows').remove(); ajax_open_block("tickets-windows" , "open-tickets-admin&idt="+idt); }); /*tickets comment*/ $('body').on('click', '[data-tid]', function () {  var comment_id = $(this).data("tid"); var comment_text = $( ".comment_text_" + comment_id ).html(); $('#tickets-reply').val( $('#tickets-reply').val()+'\n > '+comment_text+'\n'); }); $(".nav-tabs").on("click", "a", function(e){  e.preventDefault(); $(this).tab('show'); }) .on("click", ".remove-tab", function () {  var anchor = $(this).parent('a'); console.log(anchor.attr('href')); $(anchor.attr('href')).remove(); $(this).parent().parent().remove(); $(".nav-tabs li").children('a').first().click(); }); function ajax_open_block ( act , options){  $.ajax({  url: "/data/controllers", data: 'module=block_wedget_support&act=' + options, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); } }).done(function (msg) {  if(msg.type == 'msg'){  $.bootstrapGrowl( msg.text , {  type: msg.status, delay: msg.time, allow_dismiss: true }); }else if( msg.title != undefined ){  $('.nav-tabs').append('<li><a href="#'+act+'">'+ msg.title +' <i class="fa fa-times remove-tab"></i></a></li>'); $('.tab-content').append( '<div class="tab-pane" id="'+act+'">'+ '<div class="block-content-full">' +msg.html+ '</div></div>' ); $('[href = "#'+act+'"]').trigger('click'); }else{  console.log(msg); } if(msg.location){  setTimeout("document.location.href='/"+msg.location+"'", msg.time); } if(msg.eval){  jQuery.globalEval( msg.eval ); } NProgress.done(); }); }
$('body').on('click', '[lang]', function () {  var lang = $(this).attr('lang'); $.ajax({  url: "/data/controllers", data: 'module=select_language&lang='+ lang, type: "POST", cache: false, dataType: 'json', beforeSend: function () {  NProgress.start(); }, error: function (xhr, ajaxOptions, thrownError) {  NProgress.done(); console.log("Result : " + xhr.responseText); } }).done(function (msg) {  if (msg.type == 'msg') {  if (msg.status == 'success') { $.cookie('lang', lang, { expires: 5, path: '/'});} $.bootstrapGrowl(msg.text, {  type: msg.status, delay: msg.time, allow_dismiss: true }); } if(msg.location){ setTimeout("location.reload();", msg.time);} if(msg.eval){ jQuery.globalEval( msg.eval );} }).always(function () {  NProgress.done(); }); });
}); NProgress.done();
</script>
#end
</body>
</html>